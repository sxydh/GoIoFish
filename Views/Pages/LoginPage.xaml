<Page x:Class="GoIoFish.Views.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewModels="clr-namespace:GoIoFish.ViewModels"
      xmlns:models="clr-namespace:GoIoFish.Models"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <Grid Background="White" d:DataContext="{d:DesignInstance Type=viewModels:LoginViewModel}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding State, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static models:LoginState.LoadingPage}}">
            <ProgressBar IsIndeterminate="True" Width="100" Height="20" />
        </StackPanel>
    </Grid>
</Page>